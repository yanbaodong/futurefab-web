import { MutationTree } from 'vuex';
import { States } from './interface';

// mutations 处理同步数据
const mutations: MutationTree<any> = {
    DEMO: (state: States, data: States) => {
        
    }
};

export default mutations;
