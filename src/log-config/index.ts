import {
    PROJECTNAME,
    SEARCH,
    ADD,
    DELETE,
    SAVE,
    REFRESH,
    IMPORT,
    EXPORT,
    ADDVIRTUALEVENT,
    ADDVIRTUALWVENTPARAMTER,
    setButtons,
    FIELDSEPARATOR,
} from './constant';
const commonButtonList = [ADD, DELETE, SAVE, REFRESH, IMPORT, EXPORT];
